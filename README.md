# python-dl-course-unifr

Starter Python project for UniFr deep learning coursework.

## Quick start

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
pytest
python -m python_dl_course_unifr
```
